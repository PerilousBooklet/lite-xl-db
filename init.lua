--mod-version:3
local core = require"core"
local common = require"core.common"
local config = require"core.config"

-- TODO: Tableview is a tab view
-- TODO: there can be multiple TableView instances (one table data object for eacg TableView instance)

-- TODO: the table and the treeview must be searchable -> write commands with context as TableView
-- TODO: make console to run SQL query (custom View in bottom drawer), show query code in left side, show query result in right side

